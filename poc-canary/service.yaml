apiVersion: v1
kind: Service
metadata:
  name: canary-backend
  namespace: demo-app

spec:
  selector:
    app: canary-backend

  ports:
    - port: 80
      targetPort: 3000

  type: NodePort
